[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "18"

# Netlify Forms — these hidden forms enable form detection on static export
# Keep in sync with Newsletter.tsx and Guides.tsx form names
